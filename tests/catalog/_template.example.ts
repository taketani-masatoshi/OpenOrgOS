/**
 * Catalog module test template — copy to tests/catalog/{name}.test.ts
 *
 * Path: tests/catalog/_template.example.ts
 * After adding tests: npm run test:registry:sync
 */
import { describeCatalogModule } from "./catalog-module-harness.js";

/** Replace with catalog id from steward/modules/readiness.yaml */
describeCatalogModule("clinic");
