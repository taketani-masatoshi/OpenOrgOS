{
  "name": "steward-os",
  "version": "0.8.0",
  "description": "Steward OS - tenant-separated management support framework",
  "type": "module",
  "bin": {
    "steward": "./src/cli.ts"
  },
  "scripts": {
    "steward": "node --import tsx src/cli.ts",
    "validate": "node --import tsx src/cli.ts validate",
    "validate:demo": "node --import tsx src/cli.ts --tenant demo validate",
    "check": "npm run validate && npm run steward -- --tenant demo validate && npm run steward -- --tenant acme validate && npm run steward -- --tenant us-demo validate && npm run steward -- --tenant sg-demo validate && npm run steward -- --tenant ee-demo validate && npm run steward -- --tenant hk-demo validate && npm run steward -- --tenant au-demo validate && npm run steward -- --tenant tw-demo validate && npm run steward -- --tenant my-demo validate && npm run steward -- --tenant cn-demo validate && npm run steward -- --tenant ae-demo validate && npm run steward -- --tenant ru-demo validate && npm run steward -- --tenant eu-demo validate && npm run steward -- modules check --all && npm run steward -- classification check",
    "daily": "npm run check && npm run steward -- pipeline run daily",
    "weekly": "npm run check && npm run steward -- pipeline run weekly",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo:inter-org": "node --import tsx scripts/seed-inter-org-demo.ts",
    "demo:standalone-org": "node --import tsx scripts/seed-standalone-org-demo.ts",
    "demo:mal-standalone": "node --import tsx scripts/seed-mal-standalone-demo.ts"
  },
  "dependencies": {
    "@independentsoft/msg": "^1.0.2",
    "commander": "^13.1.0",
    "pdfkit": "^0.18.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pdfkit": "^0.17.6",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  },
  "engines": {
    "node": ">=20"
  }
}
