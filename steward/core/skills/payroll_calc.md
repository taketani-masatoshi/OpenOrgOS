# Skill: payroll_calc（給与計算）

**Path:** `steward/core/skills/payroll_calc.md`
**Runtime:** `cli`

## CLI

```bash
npm run orgos -- operations payroll calc --month YYYY-MM
npm run orgos -- operations payroll post-journal --month YYYY-MM
```

個人別データは L2（gitignore）。tracked には集計のみ。
