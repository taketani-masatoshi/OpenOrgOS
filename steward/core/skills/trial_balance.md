# Skill: trial_balance（試算表）

**Path:** `steward/core/skills/trial_balance.md`
**Runtime:** `cli`

## CLI

```bash
npm run orgos -- ledger trial-balance --as-of YYYY-MM-DD
npm run orgos -- ledger monthly-reconcile --month YYYY-MM
npm run validate
```
