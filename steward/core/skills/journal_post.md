# Skill: journal_post（仕訳起票）

**Path:** `steward/core/skills/journal_post.md`
**Runtime:** `cli`

## CLI

```bash
npm run orgos -- ledger post --file path/to/entry.yaml
npm run orgos -- ledger post --source depreciation --month YYYY-MM
```

## 正本

- `data/finance/journal-entries.yaml`（append-only）
- `docs/org-os/general-ledger-spec.md`
