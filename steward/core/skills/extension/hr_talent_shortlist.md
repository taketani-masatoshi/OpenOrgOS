# Skill: hr_talent_shortlist

## 目的

求人票と実演結果から選考し、署名待ちの稟議まで作る。承認はしない。

## 使用 Agent

Human Resources Agent · Recruiting Agent

## runtime

`cli`

## CLI

```bash
npm run orgos -- hr talent-shortlist --posting <file> --candidates <file> --terms <file> --operator-id <id> --approver-id <id> --api-origin <url> --json
```

## 禁止

- 承認の実行
