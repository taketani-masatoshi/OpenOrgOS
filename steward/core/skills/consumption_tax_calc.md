# Skill: consumption_tax_calc（消費税集計）

**Path:** `steward/core/skills/consumption_tax_calc.md`
**Runtime:** `cli`

## CLI

```bash
npm run orgos -- tax consumption --period YYYY-MM
npm run orgos -- tax consumption-check
npm run orgos -- ledger journal backfill-tax
```

申告書は生成しません（税理士受け渡し用集計のみ）。
