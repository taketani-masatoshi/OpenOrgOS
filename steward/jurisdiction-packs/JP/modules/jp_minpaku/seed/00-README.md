# jp_minpaku seed

有効化:

```bash
# modules.yaml に id: jp_minpaku · data_root: data/minpaku/ を追加後
STEWARD_TENANT=mal npm run orgos -- modules activate jp_minpaku
```

届出申請: `operations permit-app create --type pt-minpaku-notification`
