# jp_etax seeds

Copy examples into tenant `data/etax/` only when the module is enabled.

Do not put certificates, PINs, or production credentials here.
`credentials.yaml.example` lists certificate location fields only — PIN/password stay on the NTA module / device.
