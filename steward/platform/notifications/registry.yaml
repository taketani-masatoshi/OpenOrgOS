version: "1"

channels:
  webhook:
    # url: https://example.com/orgos/hook
    # secret: change-me
    events:
      - pipeline_daily_complete
      - approval_pending
      - company_events_monthly_audit
  openwebui:
    # ingest_url: http://127.0.0.1:3000/api/v1/ingest
    events:
      - pipeline_daily_complete
