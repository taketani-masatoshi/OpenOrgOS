# Inter-org contract protocol block — copy into CTR-*.yaml when peer wire/witness applies.
# Bind on approve: maybeBindWitnessPoolFromContract() · SLA: protocol relay reconcile

protocol:
  resilience_sla: silver
  witness_hubs:
    - hub_id: HUB-A
      hub_url: http://127.0.0.1:9474
    - hub_id: HUB-B
      hub_url: http://127.0.0.1:9475
  # witness_trust_bundle_url: https://trust.example.org/protocol/v1/trust/bundle
  # witness_trust_authority_url: https://trust.example.org
  # relay_org_uri: org:mal:relay
  # peer_id: PEER-001
  # allowed_event_types: [org.transaction.recorded, org.identity.presented]
  # allowed_transaction_types: [committee.contract.execution.notice]
  # allowed_payload_namespaces: [steward.contract]
