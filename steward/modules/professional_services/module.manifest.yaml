id: professional_services
required_seeds:
- invoice-ps-monthly.yaml
- invoice-ps-monthly-body.txt
notes: production_ready — projects + invoice seed
activation_seeds:
- projects.yaml
cli_commands:
  - show
  - validate
  - project_billing
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
