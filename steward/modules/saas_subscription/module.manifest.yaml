id: saas_subscription
required_seeds:
- invoice-saas-monthly.yaml
- invoice-saas-monthly-body.txt
notes: production_ready — plans/subscriptions + invoice
activation_seeds:
- plans.yaml
- subscriptions.yaml
cli_commands:
  - show
  - validate
  - renewals
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
