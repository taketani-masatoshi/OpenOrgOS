id: construction
required_seeds:
  - invoice-construction-monthly.yaml
  - invoice-construction-monthly-body.txt
notes: production_ready — projects + invoice seed
activation_seeds:
- phases.yaml
- sites.yaml
cli_commands:
  - show
  - validate
  - site-progress
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
