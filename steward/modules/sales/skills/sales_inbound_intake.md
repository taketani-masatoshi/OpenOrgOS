# Skill: sales_inbound_intake（CLI 実行）

## Runtime

**cli** — `npm run orgos -- skills run sales-inbound-intake`

## 目的

メール triage（routing: sales_inbound）から問合せを取り込む。

実装: `orgos operations sales inbound intake`（`--dry-run` 可）
