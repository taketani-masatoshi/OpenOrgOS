id: property_management
required_seeds:
  - invoice-pm-monthly.yaml
  - invoice-pm-monthly-body.txt
notes: production_ready — pm-properties/contracts + invoice seed
activation_seeds:
- pm-properties.yaml
- management-contracts.yaml
- fee-schedules.yaml
- service-requests.yaml
cli_commands:
  - show
  - validate
  - open-requests
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
