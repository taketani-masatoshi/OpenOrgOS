id: retail_store
required_seeds:
  - invoice-retail-monthly.yaml
  - invoice-retail-monthly-body.txt
notes: production_ready — inventory/sales + invoice seed
activation_seeds:
- skus.yaml
- stores.yaml
cli_commands:
  - show
  - validate
  - margin
  - consume-propose
  - reorder-propose
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: true
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
