id: clinic
required_seeds:
  - invoice-clinic-monthly.yaml
  - invoice-clinic-monthly-body.txt
notes: production_ready — patients/appointments + invoice seed
activation_seeds:
- appointments.yaml
- departments.yaml
cli_commands:
  - show
  - validate
  - appointments
