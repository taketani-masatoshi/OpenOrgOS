id: event_space
required_seeds:
  - invoice-event-space-monthly.yaml
  - invoice-event-space-monthly-body.txt
notes: production_ready — bookings/spaces + invoice seed
activation_seeds:
- bookings.yaml
- spaces.yaml
cli_commands:
  - show
  - validate
  - utilization
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
