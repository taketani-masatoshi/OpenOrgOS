id: staffing
required_seeds:
  - invoice-staffing-monthly.yaml
  - invoice-staffing-monthly-body.txt
notes: production_ready — assignments/staff + invoice · operations staffing CLI
activation_seeds:
- assignments.yaml
- staff.yaml
cli_commands:
  - show
  - validate
  - assignments
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
