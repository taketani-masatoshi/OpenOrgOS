id: event_operations
required_seeds:
  - invoice-event-ops-monthly.yaml
  - invoice-event-ops-monthly-body.txt
notes: production_ready — events/run_of_show + invoice seed
activation_seeds:
- events.yaml
- run_of_show.yaml
- vendors.yaml
- staff_shifts.yaml
cli_commands:
  - show
  - validate
  - runbook
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
