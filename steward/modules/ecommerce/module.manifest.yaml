id: ecommerce
required_seeds:
  - invoice-ecommerce-monthly.yaml
  - invoice-ecommerce-monthly-body.txt
notes: production_ready — orders/products + invoice seed · operations ecommerce CLI
activation_seeds:
  - orders.yaml
  - products.yaml
cli_commands:
  - show
  - validate
  - fulfillment
security:
  ai:
    can_observe: true
    can_analyze: true
    can_draft: true
    can_propose: false
    can_approve: false
    can_execute: false
  limits:
    concurrent_jobs: 2
