# Skill: language_bridge_validate（CLI 実行）

## Runtime

**cli** — `npm run orgos -- skills run language-bridge-validate`

## 目的

`language-bridge.yaml` を検証する。実装: `orgos operations locale-bridge validate`
