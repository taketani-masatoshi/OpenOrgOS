# Google Calendar push (Phase SEC-3 · gitignore)
# Copy to .env and fill values — never commit .env

GOOGLE_CALENDAR_ID=primary
GOOGLE_CALENDAR_ACCESS_TOKEN=
# Optional
GOOGLE_CALENDAR_TIMEZONE=Asia/Tokyo
