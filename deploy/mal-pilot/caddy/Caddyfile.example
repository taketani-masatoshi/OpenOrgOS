# Mode A — TLS termination at reverse proxy (ACME)
# MAL Wire Gateway — production (oorgos.org single CNAME)
# Replace wire.oorgos.org if using a dedicated org domain.

wire.oorgos.org {
  reverse_proxy 127.0.0.1:8443
}
