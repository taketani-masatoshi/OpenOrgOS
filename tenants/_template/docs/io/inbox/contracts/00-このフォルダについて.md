# 契約書（MD）

投入先: `docs/io/inbox/contracts/`

- **MD / TXT**（契約概要）
- PDF 原本は保管可だが `orgos ingest parse` は **金額仕訳しない**
- `orgos ingest review` に契約起票候補として一覧 → `data/contracts/CTR-*.yaml` は手作業 / Contract Agent
