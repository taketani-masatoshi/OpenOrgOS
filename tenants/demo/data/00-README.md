# data/ — 正データカタログ（テナント雛形）

**論理パス:** CLI · Agent では `data/` と表記（物理: `tenants/{id}/data/`）。

最低限 `company.yaml` を整備し、`npm run steward -- --tenant {id} validate` を通す。

正本カタログの完全版: [tenants/mal/data/00-README.md](../mal/data/00-README.md)
