# Extension — jp_permit_application

**Zone B** · モジュール `jp_permit_application`（Compliance proxy）有効化時に作成。

正本 Agent: `steward/jurisdiction-packs/JP/modules/jp_permit_application/agent.md`

| ファイル | 内容 |
|---------|------|
| `application-registry.yaml` | 申請案件 `APP-*` |
| `drafts/` | prepare 作業ドラフト |
| `handoffs/` | 行政書士 handoff |

保有許可 `PER-*` は `../permit-registry/`（`jp_permit_registry`）。
