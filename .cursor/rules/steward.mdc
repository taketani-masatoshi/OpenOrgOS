---
description: Steward OS data conventions and operational rules
globs:
  - cursor/**/*
  - docs/**/*
alwaysApply: false
---

# Steward OS 運用ルール

## 4 層アーキテクチャ

Steward OS は **1 体 AI が全情報を読む設計にしない**。

```
Steward → Agent（11_agents/）→ Skill（12_skills/）→ Data（cursor/data/ · docs/）
```

- **Steward:** `docs/reports/agent-summaries/` + dashboard のみ原則読取
- **Agent:** 部門フォルダを読み Skill を呼ぶ
- **Skill:** 限定入出力（[12_skills/](../../12_skills/)）
- **原則:** [13_rules/steward_os_principles.md](../../13_rules/steward_os_principles.md)

## 分け方の原則

**「誰が読むか」でフォルダを決める。** CLI が生成したからといって Cursor フォルダに置かない。

| ゾーン | パス | 中身 |
|--------|------|------|
| **人** | `docs/` | MD・CSV・**PDF**（読む・印刷・提出） |
| **Cursor** | `cursor/` | **YAML 正データ** + scratch 試行のみ |
| **プログラム** | `src/` `schemas/` `assets/` | TS・フォント等 |

## Cursor ゾーン（`cursor/`）

| フォルダ | 用途 |
|---------|------|
| `cursor/data/` | 正データ（.yaml） |
| `cursor/scratch/` | 中間試行（gitignore） |

**フォルダ説明ファイル:** `cursor/`・`assets/` は `00-README.md`、ルートのみ `README.md`。

**PDF・生成物・フォントは cursor/ に置かない。**

## 人ゾーン（`docs/`）

| フォルダ | 用途 |
|---------|------|
| `docs/plans/` | 決算書・予実 MD |
| `docs/corporate/` | 法人書類 MD |
| `docs/inbox/` | **受信トレイ**（スキャン・申請書・未処理） |
| `docs/outbox/` | **出力トレイ**（印刷・提出・掲示 PDF） |
| `docs/contracts/` | 契約書 MD |
| `docs/operations/` | 業務台帳テンプレ・プライバシー（記入済は records/） |
| `docs/data/` | CSV |
| `docs/reports/` | CLI 生成 MD |

**フォルダ説明ファイル:** 各フォルダの索引は `00-このフォルダについて.md`（エクスプローラで先頭に並ぶ）。

## 正データ（`cursor/data/`）

- 会社: `company.yaml`
- 物件: `properties/PROP-{NNN}.yaml`
- 契約: `contracts/CTR-{NNN}.yaml`
- 月次: `finances/monthly/{YYYY-MM}.yaml`
- 計画: `plans/*.yaml`
- I/O 台帳: `document-io.yaml`（inbox/outbox キュー）

## レポート生成

- MD → `docs/reports/`
- PDF → `docs/outbox/corporate/`（`npm run steward -- report kessan|jigyo`）
- フォント → `assets/fonts/`

## データ入力

1. `cursor/data/` YAML 更新 → `npm run validate`
2. スキャン受信 → `docs/inbox/` または `steward io inbox add`
3. `npm run steward -- sync all`（CSV 利用時）
4. `npm run steward -- io status`（受信/出力確認）
5. 試行は `cursor/scratch/` → 確定後 `docs/` または `cursor/data/` へ

## Agent / Skill 参照

**8 Agent:** [11_agents/00-このフォルダについて.md](../../11_agents/00-このフォルダについて.md)

| Agent | 用途 |
|-------|------|
| Executive Steward | 日次経営 · dashboard |
| Secretary | 社長スケジュール · 1-on-1 · 社外窓口 |
| Finance | 月次収支 · 税務 · 固定資産 |
| Contract | 契約台帳 · 期限 |
| Property Rental | 番町 PROP-001 |
| Hospitality | 亀沢 PROP-002 |
| Compliance | 規程 · 許認可 · ISO |
| Operations | inbox · outbox |

- Skill: [12_skills/](../../12_skills/00-このフォルダについて.md)
- ルール: [13_rules/](../../13_rules/steward_os_principles.md)
- Steward ↔ Secretary 境界: [secretary_steward_boundary.md](../../13_rules/secretary_steward_boundary.md)
