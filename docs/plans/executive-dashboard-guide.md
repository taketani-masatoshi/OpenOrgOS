# 経営ダッシュボード KPI 定義

オーナー向け日次ダッシュボード（`steward dashboard`）で表示する指標の定義と読み方。

---

## 更新頻度

- **1日1回** — `npm run steward -- dashboard` を実行
- 出力: `docs/reports/dashboard/YYYY-MM-DD.md`（日付ファイルで上書き防止）

---

## キャッシュフロー指標

| KPI | 算式 | データソース | 備考 |
|-----|------|-------------|------|
| **ランウェイ** | 現預金残高 ÷ 月次ネットバーン | 残高 TBD | 残高未登録時は算出不可 |
| **バーンレート** | 月次支出（返済含む）− 月次収入 | 月次実績 or 計画 | 正=キャッシュ流出 |
| **月次売上** | 収入合計 | `finances/monthly/` or 予実 | ベース月をダッシュボードに表示 |
| **月次利益** | 売上 − 経費 − 借入返済 | 同上 | 営業利益の近似（減価償却含む） |
| **固定費** | 本社固定費 + 減価償却 + 借入返済 | `fixed-costs.yaml` + `expense-plan` + `loans` | 売上に連動しない部分 |
| **変動費** | 物件運営費・管理手数料等 | `property-revenue` + 予実 | 売上連動の見込み |
| **損益分岐売上** | 固定費 ÷ 限界利益率 | 上記から算出 | 限界利益率 = (売上−変動費)/売上 |

### 限界利益率（貢献利益率）

賃貸・旅館の混合事業では、計画ベースの売上と変動費から全体の限界利益率を算出する。

```
限界利益率 = (月次売上 − 月次変動費) / 月次売上
損益分岐売上 = 月次固定費 / 限界利益率
```

亀沢旅館は稼働率・ADR に連動、番町ハイムは主に固定賃料収入。

---

## タスク優先度

| 区分 | 条件 |
|------|------|
| **高重要度** | 保険 draft（CTR-013/014）、high リスク契約、資金計画 TBD |
| **高緊急度** | 契約期限 30 日以内、high リスクアラート、Inbox 未処理 |

アラートは `steward alerts` と同一ロジック（`src/lib/alerts.ts`）。

---

## 月次トレンドの読み方

予実 YAML（`cursor/data/plans/yojitsu-fy*.yaml`）の月次行から:

1. **開業前** — 番町賃料のみ、設備投資（capex）集中月は単月赤字が計画内
2. **開業後** — 亀沢売上加算、運営費（変動費）発生
3. **前月比** — 直近稼働月の売上・純増減をナラティブで要約

### ナラティブテンプレート（月次レビュー用）

```
【{月}】売上 {金額} / 支出 {金額} / 純増減 {金額}
・前月比: {増減}（要因: {稼働率/空室/capex/一時費用}）
・計画比: {予実差} — {コメント}
・翌月フォーカス: {アクション1件}
```

---

## TBD 項目（データ整備待ち）

| 項目 | 登録先（案） | 影響 |
|------|-------------|------|
| 現預金残高 | `cursor/data/finances/cash-balance.yaml`（未作成） | ランウェイ |
| 返済スケジュール詳細 | `business-plan.yaml` / LOAN 返済表 | 長期 CF |
| 翻訳・サービス収支 | 月次 YAML | 売上構成比 |
| 保険証券 | CTR-013/014 executed | リスク |

---

## 関連コマンド

```bash
npm run steward -- dashboard          # 本ダッシュボード
npm run steward -- report dashboard   # 同上（report 配下）
npm run steward -- forecast           # 12ヶ月 CF 予測
npm run steward -- alerts             # 契約期限
npm run steward -- status             # データ成熟度
```

詳細キャッシュフロー表: [cashflow-detail.md](cashflow-detail.md)
